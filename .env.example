# Copy to .env and adjust if needed. Launch-CRM.bat uses these defaults if .env is missing.
# Ports (launcher uses 3002 for backend by default to avoid conflict with npm run dev on 3000)
CRM2_BACKEND_PORT=3002
CRM2_FRONTEND_PORT=5173
CRM2_POSTGRES_PORT=5434
CRM2_OLLAMA_PORT=11435

# Optional: Google SSO (leave empty if not using)
# CRM2_GOOGLE_CLIENT_ID=
# CRM2_GOOGLE_CLIENT_SECRET=
